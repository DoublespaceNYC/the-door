export type ILocale = 'en' | 'es' | 'fr'

interface Props {
  locales: ILocale[]
}

const PageNavLanguage = ({ locales }: Props): JSX.Element => {
  return <div></div>
}

export default PageNavLanguage
